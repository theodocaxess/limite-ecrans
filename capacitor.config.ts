import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theo.limitesolal',
  appName: 'Limite ecrans Solal',
  webDir: 'www'
};

export default config;
